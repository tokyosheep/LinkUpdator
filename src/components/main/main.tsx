import React from 'react';

import { MainPageContainer } from '../../styles/container';
const { MainContainer } = MainPageContainer;

export const MainContent = () => {
    return (
        <MainContainer>

        </MainContainer>
    )
}